#include <stdio.h>
/**
 * main - entry point
 * Description: Prints the last digit of a randomly generated number
 * Return: Always 0 (success)
 */
int main(void)
{
char letter = 'a';
while (letter <= 'z')
{
putchar(letter);
letter++;
}
putchar('\n');
return (0);
}

